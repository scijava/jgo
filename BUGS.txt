```
$ jgo --link=copy --links=copy sync
╭─ Error ──────────────────────────────────╮
│ No such command '--links=copy'.          │
╰──────────────────────────────────────────╯
```

`links = copy` in jgo.toml `[settings]` is not respected
`link = copy` in jgo.toml is silently ignored but should warn

Add py.typed file to package root for static type analyzers

Use https://github.com/ross/requests-futures
to do asynchronous parallel downloads of Maven artifacts?

Endpoints consist of Components right now -- this is wrong, it needs to be Artifacts/Dependencies -- because you should be able to mix in GACPVs with non-default classifier and/or packaging! (Well, packaging is arguable...)

Remove or repurpose obsolete files in docs/

Decide about remaining `~/.jgorc` references throughout the codebase

No API for managing jgo entrypoints

Do we still need --module-path-only flag?
- I lean toward yes, for symmetry, better PoLA adherence, and fail-fast when JAR(s) are non-modularizable.

What is the difference between `jgo -u` and `jgo update`? Do we need both?
- For non-project mode, `jgo -u` seems needed for whether to update hashed env...

pom-scijava bug: java3d-core 1.7.2 depends on gluegen-rt-main and jogl-all-main, and java3d-utils depends on joal-main; the `-main` artifacts each bring in *all* platform JARs :-(
